version: '3.7'
services:
  npc:
    hostname: npc
    image: ffdfgdfg/npc:0.25.3
    container_name: npc
    restart: unless-stopped
    volumes:
      - ./conf:/conf
    networks:
      - docker-network
  
networks:
  docker-network:
    name: docker-network # 网络名称
    driver: bridge
  